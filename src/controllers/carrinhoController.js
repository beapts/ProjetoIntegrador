
const carrinhoController = {
    carrinho: (req, res) => {
		res.render('finalizarCompra');
	}
}

/* const carrinhoController = {
    carrinho: (req, res) => {
		res.render('carrinho');
	}
} */

module.exports = carrinhoController